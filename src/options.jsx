import React from 'react'
import ReactDOM from 'react-dom/client'
import AppOptions from './AppOptions.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <AppOptions />
    </React.StrictMode>,
)
